#include <Arduino.h>

/**
 * PREPROCESSOR VALUES ... ?
 * 
 *  idleTime - time in seconds to stay idle before going into preflight state
 *  
 * 
 */

void setup() {

}

void loop() {
  /*
  switch(state) {
    default:          // if we hit the default state record the error somehow then shut down
      
      break;
    case IDLE:        // wait until idle period ends then go to preflight
      
      break;
    case PREFLIGHT:   // wait for the rocket to go up then go to ascending
      
      break;
    case ASCENDING:   // if the rocket starts going down switch to descending
    case DESCENDING:  // record data and fire appropriate charges. when rocket hits ground switch to postflight
      
      break;
    case POSTFLIGHT:  // do nothing
      asm("nop");
      break;
  }
  */
}